import { SafeHtmlDirective } from './safe-html.directive';

describe('SafeHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
